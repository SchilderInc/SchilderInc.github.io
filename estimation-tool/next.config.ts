import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Output as standalone for container/Vercel deployment
  // Remove this line if deploying directly to Vercel without Docker
  // output: 'standalone',
}

export default nextConfig
